module.exports = class {
  constructor(rice) {
    this.rice = rice;
  }
  async run(guild, user, rice) {
    //Doesnt work yet
    /*console.log(user.username);
        const logs = await guild.getAuditLogs({
            limit: 1,
            before: 'MEMBER_BAN_ADD',
            actionType: 22
        })*/
  }
};
